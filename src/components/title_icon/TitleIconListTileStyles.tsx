import styled from "styled-components";
import { Row } from "../../styles/layout/Flex";

const TitleIcon = styled(Row)`
  justify-content: space-between;
  align-items: start;
  width: 100%;
`;

const Title = styled(Row)`
  align-items: start;
  font-size: 40px;
  color: white;
`;

export { TitleIcon, Title };
